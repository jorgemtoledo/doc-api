{
  "name": "doc-api-solides",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.53"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs -p process.env.PORT || 8080",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
